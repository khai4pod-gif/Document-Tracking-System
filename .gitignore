# Uploaded user content — runtime data, not source
uploads/documents/*
uploads/manifests/*
!uploads/documents/.gitkeep
!uploads/manifests/.gitkeep

# Machine-specific mail credentials — never publish SMTP passwords.
# Copy config/mail.local.example.php to config/mail.local.php instead.
config/mail.local.php

# OS / editor cruft
Thumbs.db
.DS_Store

# PHPUnit / Composer
/vendor/
/.phpunit.cache/
